[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dhd-nexus"
version = "0.1.0"
description = "DHD Nexus Educational Platform"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["sdk"]